# Code Audit — 2026-02-16

Three parallel audits run against the codebase using:
- `vercel-react-best-practices` (performance & Next.js patterns)
- `web-design-guidelines` (UI/UX & accessibility)
- `vercel-composition-patterns` (component architecture)

---

## CRITICAL — Fix these first

| # | Area | Issue | File(s) |
|---|------|-------|---------|
| 1 | Performance | **3 sequential `await fetch` calls** — should be `Promise.all` | `app/dinners/[id]/page.tsx:106–154` |
| 2 | Performance | **JS-side sorting on full DB result** — no `ORDER BY`, no pagination | `app/api/bottles/route.ts:46–55` |
| 3 | Performance | **Barrel import from `@/components`** pulls entire component tree into bundles | `app/bottles/page.tsx:6`, `app/dinners/[id]/page.tsx:6` |
| 4 | Accessibility | **`<Input>` label has no `htmlFor` / `<input>` has no `id`** — screen readers can't associate them | `components/Input.tsx:14–27` |
| 5 | Accessibility | **`alert()` for validation errors** — blocks main thread, inaccessible, visually inconsistent. Use `ToastProvider` instead | `app/profile/page.tsx:77,83,137` |
| 6 | Accessibility | **Emoji-only nav buttons** (`←`, `🏠`) with no `aria-label` and sub-44px touch targets | `components/Header.tsx`, `app/dinners/[id]/rankings/page.tsx:77–86` |
| 7 | React 19 | **`forwardRef` used in `Input` and `Textarea`** — deprecated in React 19, `ref` is now a plain prop | `components/Input.tsx`, `components/Textarea.tsx` |
| 8 | Architecture | **`isAdmin` boolean prop controls entire rendering branches** in `PaymentsSection` — boolean prop proliferation. Create `AdminPaymentsSection` and read-only `PaymentsSection` variants | `components/PaymentsSection.tsx:51,87,447` |
| 9 | Architecture | **`useBackButton` boolean on `Header`** — two distinct navigation modes should be two components: `HeaderWithBack` and `HeaderWithLink` | `components/Header.tsx:8,31–43` |

---

## WARNING

| # | Area | Issue | File(s) |
|---|------|-------|---------|
| 10 | Performance | **`useDinner(id)` fetches ALL dinners then filters client-side** — add `GET /api/dinners/[id]` or use React Query cache as `initialData` | `lib/hooks/useApi.ts:39–52` |
| 11 | Performance | **`URLSearchParams` built outside `queryFn`** — stale closure on retry/refetch; move inside `queryFn` | `lib/hooks/useApi.ts:135–139` |
| 12 | Performance | **`Array.find` on static `wineTypes` inside render loop** — hoist array and a `Map` outside the component | `app/bottles/page.tsx:37–40,410` |
| 13 | Performance | **`<Image fill>` missing `sizes` prop** — Next.js defaults to `100vw` and downloads oversized images for small cards | `app/bottles/page.tsx:280,363` |
| 14 | Performance | **Mutations call full `fetchData()` manually** instead of `queryClient.invalidateQueries` | `app/dinners/page.tsx`, `app/dinners/[id]/page.tsx` |
| 15 | Accessibility | **Sort/filter toggle buttons missing `aria-pressed`** — screen readers can't tell which option is active | `app/bottles/page.tsx:66–243` |
| 16 | Accessibility | **Tailwind v3/v4 gradient utilities mixed** (`bg-gradient-to-*` vs `bg-linear-to-*`) — standardize on one | multiple files |
| 17 | Accessibility | **`globals.css` body `font-family` override** prevents Geist font from ever applying — remove or use `var(--font-geist-sans)` | `app/globals.css:25` |
| 18 | Accessibility | **No `metadata` export on any page** — browser tabs are indistinguishable, no SEO. Add `export const metadata = { title: '...' }` | all pages |
| 19 | Architecture | **`ProfilePage` is 470-line monolith** — extract `ProfileProvider`, `ProfileHeader`, `RecentRatings`, `BottlesBrought` | `app/profile/page.tsx` |
| 20 | Architecture | **Grid + list view JSX duplicated** for bottles — extract `<BottleCard variant="grid\|list">` | `app/bottles/page.tsx:269–423` |
| 21 | Architecture | **Mixed server state + modal/form UI state** in `PaymentsSection` — extract `usePayments()` hook + `<FineModal>` | `components/PaymentsSection.tsx:52–64` |
| 22 | Architecture | **`useContext` instead of `use()`** in React 19 | `components/ToastProvider.tsx:121–127` |
| 23 | Architecture | **Index used as `key` for ratings list** — unstable on reorder/add/remove; add `id` to `Rating` interface and API response | `app/dinners/[id]/rankings/page.tsx:155` |
| 24 | Architecture | **`useMemo` used for `localStorage` one-time read** — use `useState` lazy initializer instead: `useState(() => getUser()?.role ?? null)` | `app/dinners/page.tsx:44–47` |
| 25 | Architecture | **`fetchProfile` not wrapped in `useCallback`** — stale closure risk; also full profile refetch after photo upload (update local state instead) | `app/profile/page.tsx:33–64` |

---

## INFO — Low priority / polish

| # | Area | Issue | File(s) |
|---|------|-------|---------|
| 26 | Performance | No `React.cache()` wrapping DB query functions — duplicate queries possible per request | `lib/db.ts` |
| 27 | Performance | `suppressHydrationWarning` on `<body>` masks all child hydration errors — scope to specific element or remove | `app/layout.tsx:30` |
| 28 | Performance | `localStorage.getItem` called 4–5× per page — use `getUser()`/`getAuthToken()` from `lib/auth-client.ts` and cache in `useState(() => ...)` | `app/dinners/[id]/page.tsx`, others |
| 29 | Accessibility | `text-[10px]` used repeatedly — below 12px minimum; use `text-xs` at minimum | `app/profile/page.tsx` |
| 30 | Accessibility | `animate-bounce` on home logo runs indefinitely — violates WCAG 2.2.2 (> 5s animation); use `animation-iteration-count: 1` or gate with `prefers-reduced-motion` | `app/page.tsx:30` |
| 31 | Accessibility | Loading states replace entire page including header — render `Skeletons.tsx` inline below the header instead | `app/profile/page.tsx`, `app/dinners/[id]/rankings/page.tsx` |
| 32 | Accessibility | `placeholder="josecarreira@gmail.com"` — use a generic example: `nome@exemplo.com` | `app/login/page.tsx:71` |
| 33 | Accessibility | `hover:scale-[1.02]` on card links causes layout shift — gate with `prefers-reduced-motion` | `app/bottles/page.tsx:275` |
| 34 | Architecture | `Badge` and `Card` shared components exist but pages re-implement them inline — use them | `app/profile/page.tsx`, `app/bottles/page.tsx` |
| 35 | Architecture | `favorite_wine`, `recent_ratings`, `bottles_brought` typed as `any` — unsafe chain access on line 337 can throw | `app/profile/page.tsx:22–24` |
| 36 | Architecture | `bottle` typed as `any` in map callbacks | `app/bottles/page.tsx:271,353` |
| 37 | UX | `window.history.back()` instead of `router.back()` in 2 places | `app/dinners/[id]/rankings/page.tsx:78`, `app/dinners/[id]/page.tsx:296` |
| 38 | UX | "Tiebreaker rules:" label in English in a Portuguese-language UI — translate to "Critérios de desempate:" | `app/dinners/[id]/rankings/page.tsx:193` |
| 39 | UX | `pb-24` bottom padding with no visible bottom nav to justify it | `app/profile/page.tsx:182`, `app/bottles/page.tsx:46`, `app/dinners/[id]/rankings/page.tsx:89` |
| 40 | UX | No `<meta description>` / Open Graph tags on any page | all pages |

---

## Suggested Implementation Order

### Phase 1 — Quick wins (< 1h each)
- #17 Fix `globals.css` font override
- #27 Remove `suppressHydrationWarning` from `<body>`
- #37 Replace `window.history.back()` with `router.back()`
- #38 Translate "Tiebreaker rules:"
- #32 Fix placeholder email
- #6  Add `aria-label` + `p-2` padding to nav buttons
- #4  Add `id`/`htmlFor` to `Input` component

### Phase 2 — High impact, moderate effort
- #1  `Promise.all` for parallel fetches in `dinners/[id]/page.tsx`
- #2  Push sort to DB with `ORDER BY` + add pagination to bottles API
- #5  Replace `alert()` with `ToastProvider`
- #7  Remove `forwardRef` from `Input` and `Textarea` (React 19)
- #22 Replace `useContext` with `use()` in `ToastProvider`
- #13 Add `sizes` to all `<Image fill>` usages
- #18 Add `metadata` exports to all pages
- #12 Hoist `wineTypes` array + `Map` outside component
- #24 Fix `useMemo` → `useState` lazy init for localStorage reads
- #3  Replace barrel imports with direct imports

### Phase 3 — Architectural refactors
- #8/#9   Split `PaymentsSection` and `Header` into explicit variants (remove boolean props)
- #19     Extract `ProfileProvider` + subcomponents from `ProfilePage`
- #20     Extract `BottleCard` component (deduplicate grid/list JSX)
- #21     Extract `usePayments()` hook + `<FineModal>` from `PaymentsSection`
- #10     Add `GET /api/dinners/[id]` endpoint; fix `useDinner` hook
- #14     Use `invalidateQueries` instead of manual refetch after mutations
